Pod::Spec.new do |s|
  s.name         = "RNFileSystem"
  s.version      = "1.0.0"
  s.summary      = "iOS stub for react-native-file-system"
  s.homepage     = "https://github.com"
  s.license      = "MIT"
  s.author       = { "author" => "" }
  s.platform     = :ios, "16.1"
  s.source       = { :path => '.' }
  s.source_files = "*.{h,m}"
end
